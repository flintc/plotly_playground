from setuptools import setup

setup(name='plotly_adjunct',
      version='0.0.1',
      description='Plotly templates',
      # url='http://github.com/flintc/lenz',
      license='MIT',
      packages=['plotly_adjunct'],
      install_requires=[
          'webcolors',
      ],
      zip_safe=False)
